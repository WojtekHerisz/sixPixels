const Stepper = ({ current }) => (
  <div className="flex items-center justify-between relative w-2/3 m-auto">
    <div className="absolute top-50 h-1 w-full bg-bee" />
  </div>
);

export default Stepper;
